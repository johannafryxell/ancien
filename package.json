{
  "name": "ancien",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel ./*.html",
    "predeploy": "rm -rf dist && parcel build ./*.html --public-url https://johannafryxell.github.io/ancien/",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.5.0",
    "typescript": "^4.6.3"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.5.0"
  }
}
